name: Release

# Runs on every push to main.
# - Pending changesets present  → opens/updates a "Version Packages" PR
# - "Version Packages" PR merged → generates, builds, and publishes all packages
on:
  push:
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  id-token: write   # npm publish provenance (OIDC), when enabled
  pull-requests: write

jobs:
  release:
    name: Version or publish
    runs-on: ubuntu-latest
    outputs:
      published: ${{ steps.changesets.outputs.published }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # PAT so the changeset-release branch is pushed as a real user, which
          # lets the version PR be created even when the org blocks Actions from it.
          token: ${{ secrets.PACKAGES_TOKEN || secrets.GITHUB_TOKEN }}

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      # NOTE: no `registry-url` here on purpose — it writes a temp .npmrc via
      # NPM_CONFIG_USERCONFIG that authenticates npm with NODE_AUTH_TOKEN, shadowing
      # the token the "Configure registry auth" step writes to ~/.npmrc → E404 on
      # publish. Auth must come from ~/.npmrc. (Re-add only alongside provenance.)
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      # All @fluixi packages resolve from public npm, so install needs no auth.
      # The token is only needed to publish — write it to the user ~/.npmrc, since
      # pnpm v10 ignores _authToken lines in a committed project .npmrc.
      - name: Configure registry auth
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" >> ~/.npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # The `generate` step takes ~35 min; everything else here is seconds.
      # Cache its output (the generated TS modules) keyed on the generator
      # scripts + lockfile, so we only pay that cost when the generators change.
      # To force a fresh upstream re-fetch, bump the `v1` in the key below.
      - name: Cache generated icon modules
        id: icons-cache
        uses: actions/cache@v4
        with:
          path: |
            packages/icons/*/src/icons
            packages/icons/*/src/all.ts
          key: gen-icons-v2-${{ hashFiles('scripts/**', 'pnpm-lock.yaml') }}

      - name: Fetch icon repos
        run: pnpm run fetch:icons

      - name: Copy SVGs to packages
        run: pnpm copy-icons

      - name: Generate icon modules
        if: steps.icons-cache.outputs.cache-hit != 'true'
        run: pnpm generate
        timeout-minutes: 45

      - name: Generate SVG sprites
        run: pnpm sprites

      - name: Create version PR or publish
        id: changesets
        uses: changesets/action@v1
        with:
          version: pnpm run version
          publish: pnpm run release
          title: 'chore: version packages'
          commit: 'chore: version packages'
        env:
          # GITHUB_TOKEN / PACKAGES_TOKEN: git ops + opening the "Version Packages" PR.
          GITHUB_TOKEN: ${{ secrets.PACKAGES_TOKEN || secrets.GITHUB_TOKEN }}
          # NPM_TOKEN: publishes @fluixi/* to public npm (registry from publishConfig).
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  # notify-flx-icon:
  #   name: Trigger flx-icon update
  #   needs: release
  #   if: needs.release.outputs.published == 'true'
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Dispatch update-icons event to fluixi-webcomponents
  #       uses: actions/github-script@v7
  #       with:
  #         github-token: ${{ secrets.FLUIXI_DISPATCH_TOKEN }}
  #         script: |
  #           await github.rest.repos.createDispatchEvent({
  #             owner: 'fluixi',
  #             repo: 'fluixi-webcomponents',
  #             event_type: 'update-icons',
  #             client_payload: {
  #               icon_repo_sha: context.sha,
  #               triggered_by: 'fluixi/icons release'
  #             }
  #           });
